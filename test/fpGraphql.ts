import MockAdapter from 'axios-mock-adapter';

describe('fpGraphql', () => {
    it('request success', () => {
        throw new Error();
    });

    it('request error', () => {
        throw new Error();
    });

    it('request graphql error', () => {
        throw new Error();
    });
});
