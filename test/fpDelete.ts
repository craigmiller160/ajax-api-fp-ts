import MockAdapter from 'axios-mock-adapter';

describe('fpDelete', () => {
    it('request success', () => {
        throw new Error();
    });

    it('request error', () => {
        throw new Error();
    });
});
