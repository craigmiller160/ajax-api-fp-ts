import MockAdapter from 'axios-mock-adapter';

describe('fpGet', () => {
    it('request success', () => {
        throw new Error();
    });

    it('request error', () => {
        throw new Error();
    });
});
