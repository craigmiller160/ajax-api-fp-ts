import MockAdapter from 'axios-mock-adapter';

describe('fpPost', () => {
    it('request success', () => {
        throw new Error();
    });

    it('request error', () => {
        throw new Error();
    });
});
